                <% liquor.liquorComments.reverse().forEach(function(liquorComment){ %>
                    <div class="row">
                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                            <strong><%= liquorComment.author.username %></strong>
                            <span class="pull-right"><%= Date.now();%></span>
                            <p>
                                <%= liquorComment.text %>
                            </p>
                        <% if(currentUser && liquorComment.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-xs btn-warning" 
                                href="/uniTalk/liquor/<%= liquor._id %>/liquorComments/<%= liquorComment._id %>/edit">
                                Edit
                            </a>
                            <form id="delete-form" action="/uniTalk/liquor/<%= liquor._id %>/liquorComments/<%= liquorComment._id %>?_method=DELETE" method="POST">
                                <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                            </form>
                        <% } %>
                        </div>
                    </div>
                <% }); %>