<% include ../../../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                <div class="caption-full">
                    <h4><a><%= wellingtonRegionAsk.title %></a></h4>
                    <p><%= wellingtonRegionAsk.body %></p>
                    <p>
                        <em>Submitted by <%= wellingtonRegionAsk.author.username %></em>
                    </p>
                    <% if(currentUser && wellingtonRegionAsk.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/askOffer/wellingtonRegion/ask/<%= wellingtonRegionAsk._id %>/edit">Edit</a>
                        <form id="delete-form" action="/askOffer/wellingtonRegion/ask/<%= wellingtonRegionAsk._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                        <a class="btn btn-primary btn-sm btn-block" href="#">Send email</a>
                    <% } %>
                </div>
            </div>
            <div class="well">
                <div class= "text-right">
                    <a class="btn btn-xs btn-success" href="/askOffer/wellingtonRegion/ask/<%= wellingtonRegionAsk._id %>/wellingtonRegionAskComments/new/">Add comment</a>
                </div>
                <hr>
                <% wellingtonRegionAsk.comments.reverse().forEach(function(wellingtonRegionAskComment){ %>
                    <div class="row">
                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                            <strong><%= wellingtonRegionAskComment.author.username %></strong>
                            <span class="pull-right"><%= Date();%></span>
                            <p>
                                <%= wellingtonRegionAskComment.text %>
                            </p>
                        <% if(currentUser && wellingtonRegionAskComment.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-xs btn-warning" 
                                href="/askOffer/wellingtonRegion/ask/<%= wellingtonRegionAsk._id %>/wellingtonRegionAskComments/<%= wellingtonRegionAskComment._id %>/edit">
                                Edit
                            </a>
                            <form id="delete-form" action="/askOffer/wellingtonRegion/ask/<%= wellingtonRegionAsk._id %>/wellingtonRegionAskComments/<%= wellingtonRegionAskComment._id %>?_method=DELETE" method="POST">
                                <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                            </form>
                        <% } %>
                        </div>
                    </div>
                <% }); %>
            </div>
            
            <div class= "text-right">
                    <a class="btn btn-info" href="/askOffer/wellingtonRegion/ask/">Return to all asks</a>
            </div>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>