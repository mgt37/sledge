<% include ../../../partials/header %>
<div class="container">
    <div class="row">
        <h1 style="text-align: center">Edit <%= wellingtonRegionOffer.title %></h1>
        <div style="width: 66.667%; margin: 25px auto;">
            <form action="/askOffer/wellingtonRegion/offer/<%= wellingtonRegionOffer._id %>?_method=PUT" method="POST">
                <div class="form-group">
                    <label>What are you offering?</label>
                    <input class="form-control" type="text" name="wellingtonRegionOffer[title]" value="<%= wellingtonRegionOffer.title %>">
                </div>
                <div class="form-group">
                    <label>Describe the details:</label>
                    <textarea class="form-control" rows="10" name="wellingtonRegionOffer[body]"><%= wellingtonRegionOffer.body %></textarea>
                </div>
                <div class="form-group">
                    <label>What is your contact email? (Required)</label>
                    <input class="form-control" type="text" name="wellingtonRegionOffer[contactEmail]" value="<%= wellingtonRegionOffer.contactEmail %>">
                </div>
                <div class="form-group">
                    <label>How else can you be contacted? (Optional)</label>
                    <input class="form-control" type="text" name="wellingtonRegionOffer[otherContact]" value="<%= wellingtonRegionOffer.otherContact %>">
                </div>
                <div class="form-group">
                   <label>What is the hourly rate for your offer? (enter $0 if N/A)</label>
                   <input style="width: 33.333%" class="form-control" type="number" name="wellingtonRegionOffer[hourlyRate]" value="<%= wellingtonRegionOffer.hourlyRate %>" min="0.00" step="0.50"> 
                </div>
                <div class="form-group">
                    <button class="btn btn-lg btn-primary btn-block">Submit</button>
                </div>
            </form>
            <a href="/askOffer/wellingtonRegion/offer/<%= wellingtonRegionOffer._id %>">Go Back</a>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>