<% include ../../../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                <div class="caption-full">
                    <h4><a><%= aucklandOffer.title %></a></h4>
                    <p><%= aucklandOffer.body %></p>
                    <p>
                        <em>Submitted by <%= aucklandOffer.author.username %></em>
                    </p>
                    <% if(currentUser && aucklandOffer.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/askOffer/auckland/offer/<%= aucklandOffer._id %>/edit">Edit</a>
                        <form id="delete-form" action="/askOffer/auckland/offer/<%= aucklandOffer._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                        <a class="btn btn-primary btn-sm btn-block" href="#">Send email</a>
                    <% } %>
                </div>
            </div>
            <div class="well">
                <div class= "text-right">
                    <a class="btn btn-xs btn-success" href="/askOffer/auckland/offer/<%= aucklandOffer._id %>/aucklandOfferComments/new">Add comment</a>
                </div>
                <hr>
                <% aucklandOffer.comments.reverse().forEach(function(aucklandOfferComment){ %>
                    <div class="row">
                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                            <strong><%= aucklandOfferComment.author.username %></strong>
                            <span class="pull-right"><%= Date.now();%></span>
                            <p>
                                <%= aucklandOfferComment.text %>
                            </p>
                        <% if(currentUser && aucklandOfferComment.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-xs btn-warning" 
                                href="/askOffer/auckland/offer/<%= aucklandOffer._id %>/aucklandOfferComments/<%= aucklandOfferComment._id %>/edit">
                                Edit
                            </a>
                            <form id="delete-form" action="/askOffer/auckland/offer/<%= aucklandOffer._id %>/aucklandOfferComments/<%= aucklandOfferComment._id %>?_method=DELETE" method="POST">
                                <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                            </form>
                        <% } %>
                        </div>
                    </div>
                <% }); %>
            </div>
            
            <div class= "text-right">
                    <a class="btn btn-info" href="/askOffer/auckland/offer/">Return to all offers</a>
            </div>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>