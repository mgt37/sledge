<% include ../../../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                <div class="caption-full">
                    <h4><a><%= aucklandAsk.title %></a></h4>
                    <p><%= aucklandAsk.body %></p>
                    <p>
                        <em>Submitted by <%= aucklandAsk.author.username %></em>
                    </p>
                    <% if(currentUser && aucklandAsk.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/askOffer/auckland/ask/<%= aucklandAsk._id %>/edit">Edit</a>
                        <form id="delete-form" action="/askOffer/auckland/ask/<%= aucklandAsk._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                        <a class="btn btn-primary btn-sm btn-block" href="#">Send email</a>
                    <% } %>
                </div>
            </div>
            <div class="well">
                <div class= "text-right">
                    <a class="btn btn-xs btn-success" href="/askOffer/auckland/ask/<%= aucklandAsk._id %>/aucklandAskComments/new/">Add comment</a>
                </div>
                <hr>
                <% aucklandAsk.comments.reverse().forEach(function(aucklandAskComment){ %>
                    <div class="row">
                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                            <strong><%= aucklandAskComment.author.username %></strong>
                            <span class="pull-right"><%= Date();%></span>
                            <p>
                                <%= aucklandAskComment.text %>
                            </p>
                        <% if(currentUser && aucklandAskComment.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-xs btn-warning" 
                                href="/askOffer/auckland/ask/<%= aucklandAsk._id %>/aucklandAskComments/<%= aucklandAskComment._id %>/edit">
                                Edit
                            </a>
                            <form id="delete-form" action="/askOffer/auckland/ask/<%= aucklandAsk._id %>/aucklandAskComments/<%= aucklandAskComment._id %>?_method=DELETE" method="POST">
                                <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                            </form>
                        <% } %>
                        </div>
                    </div>
                <% }); %>
            </div>
            
            <div class= "text-right">
                    <a class="btn btn-info" href="/askOffer/auckland/ask/">Return to all asks</a>
            </div>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>