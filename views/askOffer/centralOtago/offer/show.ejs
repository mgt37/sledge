<% include ../../../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                <div class="caption-full">
                    <h4><a><%= centralOtagoOffer.title %></a></h4>
                    <p><%= centralOtagoOffer.body %></p>
                    <p>
                        <em>Submitted by <%= centralOtagoOffer.author.username %></em>
                    </p>
                    <% if(currentUser && centralOtagoOffer.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/askOffer/centralOtago/offer/<%= centralOtagoOffer._id %>/edit">Edit</a>
                        <form id="delete-form" action="/askOffer/centralOtago/offer/<%= centralOtagoOffer._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                        <a class="btn btn-primary btn-sm btn-block" href="#">Send email</a>
                    <% } %>
                </div>
            </div>
            <div>
                    
            </div>
            
            <div class= "text-right">
                    <a class="btn btn-info" href="/askOffer/centralOtago/offer/">Return to all offers</a>
            </div>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>