<% include ../../../partials/header %>
<div class="container" style="width: 66.667%;>
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                
                <div class="caption-full">
                    <h3><%= centralOtagoOffer.title %></h3>
                    <p>Details: <%= centralOtagoOffer.body %></p>
                    <p>Contact email: <%= centralOtagoOffer.contactEmail %></p>
                    <p>Other contact information: <%= centralOtagoOffer.otherContact %></p>
                    <p>Hourly rate: $<%= centralOtagoOffer.hourlyRate %>/hour</p>
                    
                    <p><em>Submitted by <%= centralOtagoOffer.author.username %></em></p>
                    <% if(currentUser && centralOtagoOffer.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/askOffer/centralOtago/offer/<%= centralOtagoOffer._id %>/edit">Edit</a>
                        <form id="delete-form" action="/askOffer/centralOtago/offer/<%= centralOtagoOffer._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                    <% } %>
                </div>
            </div>
            <div class="well">
                <div class= "text-right">
                    <a class="btn btn-xs btn-success" href="/askOffer/centralOtago/offer/<%= centralOtagoOffer._id %>/centralOtagoOfferComments/new/">Add comment</a>
                </div>
                <hr>
                <% centralOtagoOffer.comments.reverse().forEach(function(comment){ %>
                    <div class="row">
                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                            <strong><%= comment.author.username %></strong>
                            <span class="pull-right"><%= Date();%></span>
                            <p>
                                <%= comment.text %>
                            </p>
                        <% if(currentUser && comment.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-xs btn-warning" 
                                href="/askOffer/centralOtago/offer/<%= centralOtagoOffer._id %>/centralOtagoOfferComments/<%= comment._id %>/edit">
                                Edit
                            </a>
                            <form id="delete-form" action="/askOffer/centralOtago/offer/<%= centralOtagoOffer._id %>/centralOtagoOfferComments/<%= comment._id %>?_method=DELETE" method="POST">
                                <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                            </form>
                        <% } %>
                        </div>
                    </div>
                <% }); %>
            </div>
            
            <div class= "text-right">
                    <a class="btn btn-info" href="/askOffer/centralOtago/offer/">Return to all offers</a>
            </div>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>