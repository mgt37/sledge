<% include ../../../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                <div class="caption-full">
                    <h4><a><%= centralOtagoAsk.title %></a></h4>
                    <p><%= centralOtagoAsk.body %></p>
                    <p>
                        <em>Submitted by <%= centralOtagoAsk.author.username %></em>
                    </p>
                    <% if(currentUser && centralOtagoAsk.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/askOffer/centralOtago/ask/<%= centralOtagoAsk._id %>/edit">Edit</a>
                        <form id="delete-form" action="/askOffer/centralOtago/ask/<%= centralOtagoAsk._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                        <a class="btn btn-primary btn-sm btn-block" href="#">Send email</a>
                    <% } %>
                </div>
            </div>
            <div class="well">
                <div class= "text-right">
                    <a class="btn btn-xs btn-success" href="/askOffer/centralOtago/ask/<%= centralOtagoAsk._id %>/centralOtagoAskComments/new/">Add comment</a>
                </div>
                <hr>
                <% centralOtagoAsk.comments.reverse().forEach(function(centralOtagoAskComment){ %>
                    <div class="row">
                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                            <strong><%= centralOtagoAskComment.author.username %></strong>
                            <span class="pull-right"><%= Date();%></span>
                            <p>
                                <%= centralOtagoAskComment.text %>
                            </p>
                        <% if(currentUser && centralOtagoAskComment.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-xs btn-warning" 
                                href="/askOffer/centralOtago/ask/<%= centralOtagoAsk._id %>/centralOtagoAskComments/<%= centralOtagoAskComment._id %>/edit">
                                Edit
                            </a>
                            <form id="delete-form" action="/askOffer/centralOtago/ask/<%= centralOtagoAsk._id %>/centralOtagoAskComments/<%= centralOtagoAskComment._id %>?_method=DELETE" method="POST">
                                <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                            </form>
                        <% } %>
                        </div>
                    </div>
                <% }); %>
            </div>
            
            <div class= "text-right">
                    <a class="btn btn-info" href="/askOffer/centralOtago/ask/">Return to all asks</a>
            </div>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>