<% include ../../../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                <div class="caption-full">
                    <h4><a><%= bayOfPlentyRegionOffer.title %></a></h4>
                    <p><%= bayOfPlentyRegionOffer.body %></p>
                    <p>
                        <em>Submitted by <%= bayOfPlentyRegionOffer.author.username %></em>
                    </p>
                    <% if(currentUser && bayOfPlentyRegionOffer.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/askOffer/bayOfPlentyRegion/offer/<%= bayOfPlentyRegionOffer._id %>/edit">Edit</a>
                        <form id="delete-form" action="/askOffer/bayOfPlentyRegion/offer/<%= bayOfPlentyRegionOffer._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                        <a class="btn btn-primary btn-sm btn-block" href="#">Send email</a>
                    <% } %>
                </div>
            </div>
            <div class="well">
                <div class= "text-right">
                    <a class="btn btn-xs btn-success" href="/askOffer/bayOfPlentyRegion/offer/<%= bayOfPlentyRegionOffer._id %>/bayOfPlentyRegionOfferComments/new">Add comment</a>
                </div>
                <hr>
                <% bayOfPlentyRegionOffer.comments.reverse().forEach(function(bayOfPlentyRegionOfferComment){ %>
                    <div class="row">
                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                            <strong><%= bayOfPlentyRegionOfferComment.author.username %></strong>
                            <span class="pull-right"><%= Date.now();%></span>
                            <p>
                                <%= bayOfPlentyRegionOfferComment.text %>
                            </p>
                        <% if(currentUser && bayOfPlentyRegionOfferComment.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-xs btn-warning" 
                                href="/askOffer/bayOfPlentyRegion/offer/<%= bayOfPlentyRegionOffer._id %>/bayOfPlentyRegionOfferComments/<%= bayOfPlentyRegionOfferComment._id %>/edit">
                                Edit
                            </a>
                            <form id="delete-form" action="/askOffer/bayOfPlentyRegion/offer/<%= bayOfPlentyRegionOffer._id %>/bayOfPlentyRegionOfferComments/<%= bayOfPlentyRegionOfferComment._id %>?_method=DELETE" method="POST">
                                <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                            </form>
                        <% } %>
                        </div>
                    </div>
                <% }); %>
            </div>
            
            <div class= "text-right">
                    <a class="btn btn-info" href="/askOffer/bayOfPlentyRegion/offer/">Return to all offers</a>
            </div>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>