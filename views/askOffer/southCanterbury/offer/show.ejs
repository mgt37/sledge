<% include ../../../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                <div class="caption-full">
                    <h4><a><%= southCanterburyOffer.title %></a></h4>
                    <p><%= southCanterburyOffer.body %></p>
                    <p>
                        <em>Submitted by <%= southCanterburyOffer.author.username %></em>
                    </p>
                    <% if(currentUser && southCanterburyOffer.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/askOffer/southCanterbury/offer/<%= southCanterburyOffer._id %>/edit">Edit</a>
                        <form id="delete-form" action="/askOffer/southCanterbury/offer/<%= southCanterburyOffer._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                        <a class="btn btn-primary btn-sm btn-block" href="#">Send email</a>
                    <% } %>
                </div>
            </div>
            <div class="well">
                <div class= "text-right">
                    <a class="btn btn-xs btn-success" href="/askOffer/southCanterbury/offer/<%= southCanterburyOffer._id %>/southCanterburyOfferComments/new">Add comment</a>
                </div>
                <hr>
                <% southCanterburyOffer.comments.reverse().forEach(function(southCanterburyOfferComment){ %>
                    <div class="row">
                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                            <strong><%= southCanterburyOfferComment.author.username %></strong>
                            <span class="pull-right"><%= Date.now();%></span>
                            <p>
                                <%= southCanterburyOfferComment.text %>
                            </p>
                        <% if(currentUser && southCanterburyOfferComment.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-xs btn-warning" 
                                href="/askOffer/southCanterbury/offer/<%= southCanterburyOffer._id %>/southCanterburyOfferComments/<%= southCanterburyOfferComment._id %>/edit">
                                Edit
                            </a>
                            <form id="delete-form" action="/askOffer/southCanterbury/offer/<%= southCanterburyOffer._id %>/southCanterburyOfferComments/<%= southCanterburyOfferComment._id %>?_method=DELETE" method="POST">
                                <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                            </form>
                        <% } %>
                        </div>
                    </div>
                <% }); %>
            </div>
            
            <div class= "text-right">
                    <a class="btn btn-info" href="/askOffer/southCanterbury/offer/">Return to all offers</a>
            </div>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>