<% include ../../../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                <div class="caption-full">
                    <h4><a><%= nelsonMarlboroughRegionOffer.title %></a></h4>
                    <p><%= nelsonMarlboroughRegionOffer.body %></p>
                    <p>
                        <em>Submitted by <%= nelsonMarlboroughRegionOffer.author.username %></em>
                    </p>
                    <% if(currentUser && nelsonMarlboroughRegionOffer.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/askOffer/nelsonMarlboroughRegion/offer/<%= nelsonMarlboroughRegionOffer._id %>/edit">Edit</a>
                        <form id="delete-form" action="/askOffer/nelsonMarlboroughRegion/offer/<%= nelsonMarlboroughRegionOffer._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                        <a class="btn btn-primary btn-sm btn-block" href="#">Send email</a>
                    <% } %>
                </div>
            </div>
            <div class="well">
                <div class= "text-right">
                    <a class="btn btn-xs btn-success" href="/askOffer/nelsonMarlboroughRegion/offer/<%= nelsonMarlboroughRegionOffer._id %>/nelsonMarlboroughRegionOfferComments/new">Add comment</a>
                </div>
                <hr>
                <% nelsonMarlboroughRegionOffer.comments.reverse().forEach(function(nelsonMarlboroughRegionOfferComment){ %>
                    <div class="row">
                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                            <strong><%= nelsonMarlboroughRegionOfferComment.author.username %></strong>
                            <span class="pull-right"><%= Date.now();%></span>
                            <p>
                                <%= nelsonMarlboroughRegionOfferComment.text %>
                            </p>
                        <% if(currentUser && nelsonMarlboroughRegionOfferComment.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-xs btn-warning" 
                                href="/askOffer/nelsonMarlboroughRegion/offer/<%= nelsonMarlboroughRegionOffer._id %>/nelsonMarlboroughRegionOfferComments/<%= nelsonMarlboroughRegionOfferComment._id %>/edit">
                                Edit
                            </a>
                            <form id="delete-form" action="/askOffer/nelsonMarlboroughRegion/offer/<%= nelsonMarlboroughRegionOffer._id %>/nelsonMarlboroughRegionOfferComments/<%= nelsonMarlboroughRegionOfferComment._id %>?_method=DELETE" method="POST">
                                <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                            </form>
                        <% } %>
                        </div>
                    </div>
                <% }); %>
            </div>
            
            <div class= "text-right">
                    <a class="btn btn-info" href="/askOffer/nelsonMarlboroughRegion/offer/">Return to all offers</a>
            </div>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>