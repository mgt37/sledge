<% include ../../../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                <div class="caption-full">
                    <h4><a><%= hawkesBayRegionOffer.title %></a></h4>
                    <p><%= hawkesBayRegionOffer.body %></p>
                    <p>
                        <em>Submitted by <%= hawkesBayRegionOffer.author.username %></em>
                    </p>
                    <% if(currentUser && hawkesBayRegionOffer.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/askOffer/hawkesBayRegion/offer/<%= hawkesBayRegionOffer._id %>/edit">Edit</a>
                        <form id="delete-form" action="/askOffer/hawkesBayRegion/offer/<%= hawkesBayRegionOffer._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                        <a class="btn btn-primary btn-sm btn-block" href="#">Send email</a>
                    <% } %>
                </div>
            </div>
            <div class="well">
                <div class= "text-right">
                    <a class="btn btn-xs btn-success" href="/askOffer/hawkesBayRegion/offer/<%= hawkesBayRegionOffer._id %>/hawkesBayRegionOfferComments/new">Add comment</a>
                </div>
                <hr>
                <% hawkesBayRegionOffer.comments.reverse().forEach(function(hawkesBayRegionOfferComment){ %>
                    <div class="row">
                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                            <strong><%= hawkesBayRegionOfferComment.author.username %></strong>
                            <span class="pull-right"><%= Date.now();%></span>
                            <p>
                                <%= hawkesBayRegionOfferComment.text %>
                            </p>
                        <% if(currentUser && hawkesBayRegionOfferComment.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-xs btn-warning" 
                                href="/askOffer/hawkesBayRegion/offer/<%= hawkesBayRegionOffer._id %>/hawkesBayRegionOfferComments/<%= hawkesBayRegionOfferComment._id %>/edit">
                                Edit
                            </a>
                            <form id="delete-form" action="/askOffer/hawkesBayRegion/offer/<%= hawkesBayRegionOffer._id %>/hawkesBayRegionOfferComments/<%= hawkesBayRegionOfferComment._id %>?_method=DELETE" method="POST">
                                <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                            </form>
                        <% } %>
                        </div>
                    </div>
                <% }); %>
            </div>
            
            <div class= "text-right">
                    <a class="btn btn-info" href="/askOffer/hawkesBayRegion/offer/">Return to all offers</a>
            </div>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>