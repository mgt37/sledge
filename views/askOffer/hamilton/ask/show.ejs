<% include ../../../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                <div class="caption-full">
                    <h4><a><%= hamiltonAsk.title %></a></h4>
                    <p><%= hamiltonAsk.body %></p>
                    <p>
                        <em>Submitted by <%= hamiltonAsk.author.username %></em>
                    </p>
                    <% if(currentUser && hamiltonAsk.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/askOffer/hamilton/ask/<%= hamiltonAsk._id %>/edit">Edit</a>
                        <form id="delete-form" action="/askOffer/hamilton/ask/<%= hamiltonAsk._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                        <a class="btn btn-primary btn-sm btn-block" href="#">Send email</a>
                    <% } %>
                </div>
            </div>
            <div class="well">
                <div class= "text-right">
                    <a class="btn btn-xs btn-success" href="/askOffer/hamilton/ask/<%= hamiltonAsk._id %>/hamiltonAskComments/new/">Add comment</a>
                </div>
                <hr>
                <% hamiltonAsk.comments.reverse().forEach(function(hamiltonAskComment){ %>
                    <div class="row">
                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                            <strong><%= hamiltonAskComment.author.username %></strong>
                            <span class="pull-right"><%= Date();%></span>
                            <p>
                                <%= hamiltonAskComment.text %>
                            </p>
                        <% if(currentUser && hamiltonAskComment.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-xs btn-warning" 
                                href="/askOffer/hamilton/ask/<%= hamiltonAsk._id %>/hamiltonAskComments/<%= hamiltonAskComment._id %>/edit">
                                Edit
                            </a>
                            <form id="delete-form" action="/askOffer/hamilton/ask/<%= hamiltonAsk._id %>/hamiltonAskComments/<%= hamiltonAskComment._id %>?_method=DELETE" method="POST">
                                <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                            </form>
                        <% } %>
                        </div>
                    </div>
                <% }); %>
            </div>
            
            <div class= "text-right">
                    <a class="btn btn-info" href="/askOffer/hamilton/ask/">Return to all asks</a>
            </div>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>