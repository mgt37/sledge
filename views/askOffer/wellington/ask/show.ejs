<% include ../../../partials/header %>
<div class="container" style="width: 66.667%;>
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                
                <div class="caption-full">
                    <h3><%= wellingtonAsk.title %></h3>
                    <p>Details: <%= wellingtonAsk.body %></p>
                    <p>Contact email: <%= wellingtonAsk.contactEmail %></p>
                    <p>Other contact information: <%= wellingtonAsk.otherContact %></p>
                    <p>Maximum hourly rate: $<%= wellingtonAsk.hourlyRate %>/hour</p>
                    
                    
                    <p><em>Submitted by <%= wellingtonAsk.author.username %></em></p>
                    <% if(currentUser && wellingtonAsk.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/askOffer/wellington/ask/<%= wellingtonAsk._id %>/edit">Edit</a>
                        <form id="delete-form" action="/askOffer/wellington/ask/<%= wellingtonAsk._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                    <% } %>
                </div>
            </div>
            <div class="well">
                <div class= "text-right">
                    <a class="btn btn-xs btn-success" href="/askOffer/wellington/ask/<%= wellingtonAsk._id %>/wellingtonAskComments/new/">Add comment</a>
                </div>
                <hr>
                <% wellingtonAsk.comments.reverse().forEach(function(comment){ %>
                    <div class="row">
                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                            <strong><%= comment.author.username %></strong>
                            <span class="pull-right"><%= Date();%></span>
                            <p>
                                <%= comment.text %>
                            </p>
                        <% if(currentUser && comment.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-xs btn-warning" 
                                href="/askOffer/wellington/ask/<%= wellingtonAsk._id %>/wellingtonAskComments/<%= comment._id %>/edit">
                                Edit
                            </a>
                            <form id="delete-form" action="/askOffer/wellington/ask/<%= wellingtonAsk._id %>/wellingtonAskComments/<%= comment._id %>?_method=DELETE" method="POST">
                                <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                            </form>
                        <% } %>
                        </div>
                    </div>
                <% }); %>
            </div>
            
            <div class= "text-right">
                    <a class="btn btn-info" href="/askOffer/wellington/ask/">Return to all asks</a>
            </div>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>