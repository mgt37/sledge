<% include ../../../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                <div class="caption-full">
                    <h4><a><%= coastalOtagoAsk.title %></a></h4>
                    <p><%= coastalOtagoAsk.body %></p>
                    <p>
                        <em>Submitted by <%= coastalOtagoAsk.author.username %></em>
                    </p>
                    <% if(currentUser && coastalOtagoAsk.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/askOffer/coastalOtago/ask/<%= coastalOtagoAsk._id %>/edit">Edit</a>
                        <form id="delete-form" action="/askOffer/coastalOtago/ask/<%= coastalOtagoAsk._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                        <a class="btn btn-primary btn-sm btn-block" href="#">Send email</a>
                    <% } %>
                </div>
            </div>
            <div>
                    
            </div>
            
            <div class= "text-right">
                    <a class="btn btn-info" href="/askOffer/coastalOtago/ask/">Return to all asks</a>
            </div>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>