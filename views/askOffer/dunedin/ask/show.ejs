<% include ../../../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                <div class="caption-full">
                    <h4><a><%= dunedinAsk.title %></a></h4>
                    <p><%= dunedinAsk.body %></p>
                    <p>
                        <em>Submitted by <%= dunedinAsk.author.username %></em>
                    </p>
                    <% if(currentUser && dunedinAsk.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/askOffer/dunedin/ask/<%= dunedinAsk._id %>/edit">Edit</a>
                        <form id="delete-form" action="/askOffer/dunedin/ask/<%= dunedinAsk._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                        <a class="btn btn-primary btn-sm btn-block" href="#">Send email</a>
                    <% } %>
                </div>
            </div>
            <div class="well">
                <div class= "text-right">
                    <a class="btn btn-xs btn-success" href="/askOffer/dunedin/ask/<%= dunedinAsk._id %>/dunedinAskComments/new/">Add comment</a>
                </div>
                <hr>
                <% dunedinAsk.comments.reverse().forEach(function(dunedinAskComment){ %>
                    <div class="row">
                        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                            <strong><%= dunedinAskComment.author.username %></strong>
                            <span class="pull-right"><%= Date();%></span>
                            <p>
                                <%= dunedinAskComment.text %>
                            </p>
                        <% if(currentUser && dunedinAskComment.author.id.equals(currentUser._id)){ %>
                            <a class="btn btn-xs btn-warning" 
                                href="/askOffer/dunedin/ask/<%= dunedinAsk._id %>/dunedinAskComments/<%= dunedinAskComment._id %>/edit">
                                Edit
                            </a>
                            <form id="delete-form" action="/askOffer/dunedin/ask/<%= dunedinAsk._id %>/dunedinAskComments/<%= dunedinAskComment._id %>?_method=DELETE" method="POST">
                                <input type="submit" class="btn btn-xs btn-danger" value="Delete">
                            </form>
                        <% } %>
                        </div>
                    </div>
                <% }); %>
            </div>
            
            <div class= "text-right">
                    <a class="btn btn-info" href="/askOffer/dunedin/ask/">Return to all asks</a>
            </div>
        </div>
    </div>
</div>
<% include ../../../partials/footer %>