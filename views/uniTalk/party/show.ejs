<% include ../../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                <div class="caption-full">
                    <h4><a><%= party.title %></a></h4>
                    <p><%= party.body %></p>
                    <p>
                        <em>Submitted by <%= party.author.username %></em>
                    </p>
                    <% if(currentUser && party.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/uniTalk/party/<%= party._id %>/edit">Edit</a>
                        <form id="delete-form" action="/uniTalk/party/<%= party._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                    <% } %>
                </div>
            </div>
            <div class="well">
                <div class= "text-right">
                    <a class="btn btn-xs btn-success" href="/uniTalk/party/<%= party._id %>/partyComments/new">Add comment</a>
                </div>
                <hr>
                <% include ../../partials/uniTalk/partyComment %>
            </div>
            <div class= "text-right">
                    <a class="btn btn-success" href="/uniTalk/party/">Return to all topics</a>
            </div>
        </div>
    </div>
</div>
<% include ../../partials/footer %>