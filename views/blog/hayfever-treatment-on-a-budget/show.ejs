<% include ../../partials/header %>
<div class="container">
    <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
            <div class="thumbnail">
                <div class="caption-full">
                    <p><%= hayfeverTreatmentComment.body %></p>
                    <p>
                        <em>Submitted by <%= hayfeverTreatmentComment.author.username %></em>
                    </p>
                    <% if(currentUser && hayfeverTreatmentComment.author.id.equals(currentUser._id)){ %>
                        <a class="btn btn-xs btn-warning" href="/blog/hayfever-treatment-on-a-budget/<%= hayfeverTreatmentComment._id %>/edit">Edit</a>
                        <form id="delete-form" action="/blog/hayfever-treatment-on-a-budget/<%= hayfeverTreatmentComment._id %>?_method=DELETE" method="POST">
                            <button class="btn btn-xs btn-danger">Delete</button>
                        </form>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</div>
<% include ../../partials/footer %>